<h1>Listing Players</h1>

<tbody>
  <% @players.each do |player| %>
  <br>
  <tr>
    <td>Player name: <%= link_to player.name, player_path(player) %></td>
    <br>
  </tr>
  <% end %>
  <h3>
    <%= link_to 'Click Here to Create a New Player', new_player_path %>
  </h3>
</tbody>

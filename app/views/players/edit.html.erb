<h1>Edit Player</h1>

<%= form_with model: @player, url: player_path(@player), method: :put, local: true do |f| %>
    <%= f.text_field :name, placeholder: 'Enter New Player Name' %>
    <%= f.submit %>
<% end %>
